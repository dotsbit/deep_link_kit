/// Suffix length for short Dynamic Links (Firebase parity).
enum ShortDynamicLinkType {
  /// Compact short code (mlink default ~8 chars).
  short,

  /// Longer unguessable code (~17 chars).
  unguessable,
}
